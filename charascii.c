// 6. Write a program which takes a character as an input and displays its ASCII code.

#include<stdio.h>

int main(){
    char ch;
    printf("Enter a character\n");
    scanf("%c",&ch);
    printf("its ASCII code is %d",ch);
    return 0;
}